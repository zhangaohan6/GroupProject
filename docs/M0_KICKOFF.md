# M0_KICKOFF.md — 启动会(7/20)

> 目标:**一次会定死所有阻塞并行的东西**,散会后 5 人能真正并行。配套 [PROJECT_PLAN.md](PROJECT_PLAN.md) · [CONTRACTS.md](CONTRACTS.md)

## 议程(建议 45 分钟)
1. **确认分组**(WebCMS)—— 擅自换组 **−50%**,先核对 5 人无误
2. **认领角色**(A 数据/评测 · B 传统 · C 从零 · D 迁移 · E 高级)—— 填下表
3. **建三样协作基建**(当场建好,别拖):GitHub 私库 + 共享 Google Drive + Overleaf(CVPR 模板)
4. **锁 CONTRACTS.md**(数据 schema / `predict_proba` 接口 / `load_image` / 结果格式)—— 逐条读一遍,拍板
5. **确认目标 28+ 两方向**:Grad-CAM + 退化鲁棒性
6. **算力**:C、D 各买一月 Colab Pro(后台执行);A/B/E 免费版够
7. **约定周会**:每周 tutor consultation(只剩约 3 次),M2、M4 后各去一次
8. **分头**:24 小时行动项(见下)

## 决议记录表(会上填)
| 项 | 决议 |
|---|---|
| 5 人名单确认 | z5633314(你) + ____ + ____ + ____ + ____ |
| A 数据/评测 | ____ |
| B 传统方法 | ____ |
| C 深度·从零 | ____ |
| D 深度·迁移(出最佳模型) | ____ |
| E 高级研究(Grad-CAM+鲁棒性) | ____(建议 = 你,打你评测/鲁棒性主场) |
| GitHub 私库 URL | ____ |
| 共享 Drive 文件夹 | ____ |
| Overleaf 项目 | ____ |
| Colab Pro 谁买 | C、D |
| seed | 42(已定) |

## 24 小时行动项
- **A(+1 人配对)**:开跑数据管线 —— 冻结 `classes_500.json`、流式抽取 500 类、缩放 256px、传 Drive、出三份 manifest。**这是唯一硬阻塞(M1=7/22)**,42GB 下载是最大单点风险,配对做。
- **A**:把仓库骨架(已就绪)push 上 GitHub,建 Overleaf 导入 `report/main.tex`。
- **B**:免费版跑通 SIFT 提取 + MiniBatchKMeans 词袋(先用少量图验证链路)。
- **C/D**:各自把 `deep_cnn.py` 在小样本上跑 1 epoch 验证环境(等 M1 真数据)。
- **E**:先造工具 —— 退化库 5 类 × 5 severity + Grad-CAM,**在现成 ImageNet 预训练模型上验证正确性**(不空等模型)。
- **全员**:读 CONTRACTS.md,有异议今天提;明天起按契约写。

## 关键提醒(散会前念一遍)
- **表达占 90% 分**(Video 13 + Report 14 = 27/30)。评分**不看指标高低**,只看收敛+合理+叙述+消融+误差分析。报告从 M2 就动笔,别压最后两天。
- **每人都有 代码+视频+报告 三类任务**(Week 11 匿名互评影响个人分)。
- **先打通竖切再优化**:M2(7/26)前每条 pipeline 都要跑出一个数字,哪怕很烂。
- CVPR 模板用错**可能直接 0 分**;视频 5 人**都要出镜报名字 + 实时摄像头**。
